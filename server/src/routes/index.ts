import { Router } from 'express';

const router = Router();

// Routes will be mounted here as features are implemented
// e.g. router.use('/posts', postsRouter);
//      router.use('/bikes', bikesRouter);
//      router.use('/auth', authRouter);

export { router };
