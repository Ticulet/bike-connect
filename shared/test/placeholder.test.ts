import { describe, it, expect } from 'vitest';

// Placeholder test — replace with real tests once the suite grows.
describe('vitest runner bootstrap', () => {
  it('discovers and executes tests in shared/', () => {
    expect(1 + 1).toBe(2);
  });
});
